// Render the circles using React!
const App = (props) => {
    let { circles } = props;
    console.log('what is props', props)
    return (
        <span>circles</span>
    )
}

const Circle = null;  // CHALLENGE: Write a separate Circle component for use in the App component
//return html of circle